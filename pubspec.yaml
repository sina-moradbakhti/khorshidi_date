name: khorshidi_calendar
description: A calendar widget using as DatePicker and Calendar as well in Khorshidi (solar) date system.
version: 0.0.1
homepage: http://github.com/sina-moradbakhti

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shamsi_date: ^1.0.1
  persian_fonts: ^1.0.5
  infinite_listview: ^1.1.0
  indexed_list_view: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
